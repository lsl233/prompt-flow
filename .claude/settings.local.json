{
  "permissions": {
    "allow": [
      "Bash(pnpm install:*)",
      "Bash(pnpm compile:*)",
      "Bash(pnpm ls:*)",
      "Bash(cp:*)",
      "WebFetch(domain:wxt.dev)",
      "WebSearch",
      "Bash(pnpm build:*)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      "echo 'Running post-edit checks...'",
      "npm run type-check 2>&1 || echo '⚠️ TypeScript errors detected'",
      "npm run lint -- --max-warnings=0 2>&1 || echo '⚠️ Lint errors detected'"
    ]
  }
}